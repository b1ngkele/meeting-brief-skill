interface:
  display_name: "会议简报生成"
  short_description: "从笔记和录音生成会议简报DOCX"
  default_prompt: "Use $meeting-brief to generate a meeting brief from notes.txt and transcript.txt while preserving the DOCX template formatting."
policy:
  allow_implicit_invocation: true
