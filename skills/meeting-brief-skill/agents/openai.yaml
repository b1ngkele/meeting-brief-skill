interface:
  display_name: "会议简报生成"
  short_description: "从笔记、录音和可选周例会材料生成会议简报DOCX"
  default_prompt: "Use $meeting-brief to generate a meeting brief from notes.txt, transcript.txt, and optionally weeklyMeetingMaterials.pdf while preserving the DOCX template formatting."
policy:
  allow_implicit_invocation: true
